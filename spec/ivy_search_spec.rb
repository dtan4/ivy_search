require 'spec_helper'

describe IvySearch do
  it 'has a version number' do
    expect(IvySearch::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
